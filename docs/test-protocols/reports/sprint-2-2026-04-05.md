# Sprint 2 Test Report — 2026-04-05

**Sprint scope:** Clipboard commands, SPL load security tests, E2E scaffolding, SSH lifecycle partial implementation (known_hosts, SecretService, SshManager guards)
**Date:** 2026-04-05
**Session:** d
**Total tests after sprint:** 208 passing, 3 skipped (ignored load tests counted separately)
**Clippy:** Clean (`cargo clippy -- -D warnings` exits 0)

---

## 1. Point 1 — Clipboard Commands

### Protocol amendments

| Document | Change |
|---|---|
| `functional-pty-vt-ssh-preferences-ui-ipc.md` | Added `TEST-IPC-CLIP-001` test scenario documenting IPC clipboard unit tests |
| `security-pty-ipc-ssh-credentials-csp-osc52.md` | Added `SEC-OSC-004` — clipboard IPC size limit enforced before arboard call |

### Implementation

| Change | Status |
|---|---|
| `copy_to_clipboard` implemented via `arboard::Clipboard` + `spawn_blocking` | Done |
| `get_clipboard` implemented via `arboard::Clipboard` + `spawn_blocking` | Done |
| `MAX_CLIPBOARD_LEN = 16 MiB` validation before arboard call | Done |
| `clipboard-all` Tauri permission NOT required (arboard is direct Rust, not tauri-plugin-clipboard-manager) | Verified |
| Commands already in `generate_handler![]` — no change needed to `lib.rs` | Verified |

### Tests

| Test ID | Result | Notes |
|---|---|---|
| `ipc_clip_001` — oversized payload rejected | PASS | Returns `CLIPBOARD_TOO_LARGE` |
| `ipc_clip_002` — empty string passes validation | PASS | arboard may fail in CI headless env |
| `ipc_clip_003` — at-limit payload passes validation | PASS | |
| `ipc_clip_004` — get_clipboard does not panic | PASS | Returns error in headless CI |

**Coverage gaps:** End-to-end clipboard round-trip (copy→paste) requires a running display server. Covered by E2E TEST-CLIP-002 (deferred).

---

## 2. Point 2 — SPL-RM-001 + SPL-SZ-004 Load Security Tests

### Protocol amendments

| Document | Change |
|---|---|
| `security-linux-pty-session.md` | `SPL-RM-001`: added `/proc/self/fd` baseline measurement procedure |
| `security-linux-pty-session.md` | `SPL-SZ-004`: added explicit 5-second timeout expectation |

### Implementation

| Change | Status |
|---|---|
| `src-tauri/src/security_load.rs` created (new module) | Done |
| `security_load` declared in `lib.rs` | Done |
| `spl_rm_001_no_fd_leak_after_10_pane_open_close` — `#[ignore]` | Done |
| `spl_sz_004_rapid_10x_64kib_validation_within_5_seconds` — `#[ignore]` | Done |
| `spl_sz_004_over_limit_rejected` — `#[ignore]` | Done |

### Tests (run with `cargo nextest run --run-ignored all`)

| Test ID | Result | Notes |
|---|---|---|
| `spl_rm_001` | PASS | Validates fd count at stub level; full check requires real PTY |
| `spl_sz_004` (timing) | PASS | 10 × 64 KiB validation completes well within 5s |
| `spl_sz_004` (boundary) | PASS | 65537-byte payload rejected |

**Coverage gaps:** `spl_rm_001` validates fd count at the `PtyBackend` creation level only. Full fd leak detection (SPL-RM-001 proper) requires real PTY sessions (currently `todo!()`). Blocked on PTY I/O implementation.

---

## 3. Point 3 — E2E Test Scaffolding (WebdriverIO)

### Protocol amendments

| Document | Change |
|---|---|
| `functional-pty-vt-ssh-preferences-ui-ipc.md` §2.4 | Added §2.4.1 E2E Harness Validation checklist (CI readiness, 95% pass-rate threshold, timeout expectations) |

### Implementation

| File | Status | Scenarios |
|---|---|---|
| `tests/e2e/app.spec.ts` | Pre-existing | App launch (2 scenarios) |
| `tests/e2e/pty-roundtrip.spec.ts` | New | PTY input/output round-trip (2 scenarios) — DEFERRED |
| `tests/e2e/tab-lifecycle.spec.ts` | New | Tab create/close lifecycle (4 scenarios) — DEFERRED |
| `tests/tsconfig.json` | Updated | Fixed to compile E2E files independently of root tsconfig |

**TypeScript type-check:** `npx tsc --noEmit -p tests/tsconfig.json` exits 0.

**Execution status:** Deferred — requires `pnpm tauri build`. Scenarios are written, reviewed, and type-correct. They will execute once the build gate is green in CI.

---

## 4. Point 4 — SSH Lifecycle (Partial)

### What was implemented

The full SSH connection lifecycle (TCP connect → russh handshake → auth → keepalive → PTY channel) was not fully implemented due to the complexity of integration-testing against a real SSH server. The following components were implemented:

| Component | Status | Notes |
|---|---|---|
| `known_hosts.rs` | Implemented | Parse, lookup (Unknown/Trusted/Mismatch), add entry, import, 0600 permissions |
| `credentials_linux.rs` | Implemented | Real SecretService via `secret-service` 5.1.0 crate |
| `SshManager` guard methods | Implemented | Duplicate connection, unknown pane errors |
| `SshManager::connection_count()` | Implemented | Utility for tests |
| `ssh/manager.rs` manager_tests module | Implemented | 6 new unit tests |
| `base64 = "0.22"` + `dirs = "6"` added to `Cargo.toml` | Done | |
| `tempfile` added as dev-dependency | Done | Used in known_hosts tests |

### Protocol amendments

| Document | Change |
|---|---|
| `functional-pty-vt-ssh-preferences-ui-ipc.md` | Added `TEST-SSH-UNIT-001` through `TEST-SSH-UNIT-005` (integration test specs) |
| `security-pty-ipc-ssh-credentials-csp-osc52.md` | Added `SEC-CRED-006`, `SEC-CRED-007`, `SEC-CRED-008` (known_hosts tampering, key material memory, is_available probe) |

### Tests

| Test ID | Result | Notes |
|---|---|---|
| `ssh_manager_rejects_duplicate_pane_connection` | PASS | |
| `ssh_manager_close_unknown_pane_returns_error` | PASS | |
| `ssh_manager_reconnect_unknown_pane_returns_error` | PASS | |
| `ssh_manager_open_then_close_cleans_up` | PASS | |
| `ssh_manager_get_state_returns_none_for_unknown_pane` | PASS | |
| `ssh_manager_new_connection_in_connecting_state` | PASS | |
| `known_hosts_parse_ed25519_entry` | PASS | |
| `known_hosts_lookup_unknown_host` | PASS | TOFU first connection |
| `known_hosts_lookup_trusted_matching_key` | PASS | |
| `known_hosts_lookup_mismatch_different_key` | PASS | SEC-CRED-006 |
| `known_hosts_add_and_lookup` | PASS | Round-trip |
| `known_hosts_skips_hashed_entries` | PASS | ADR-0007 |
| `known_hosts_skips_comments_and_empty_lines` | PASS | |
| `known_hosts_nonexistent_file_returns_empty` | PASS | |
| `credential_store_is_available_does_not_panic` | PASS | |
| `credential_store_store_fails_gracefully_when_unavailable` | PASS | Skipped in environments with keyring |
| `credential_store_get_returns_none_when_unavailable` | PASS | |
| `credential_store_delete_does_not_panic` | PASS | |

### Coverage gaps / blocked items

| Gap | Reason | Unblocked when |
|---|---|---|
| TCP connect → russh handshake | Requires network + SSH server mock | SSH integration pass |
| `auth.rs` — authentication sequence (pubkey, password, keyboard-interactive) | `auth.rs` is still a stub | SSH integration pass |
| `keepalive.rs` — keepalive timer with tokio time mock | `keepalive.rs` is still a stub | SSH integration pass |
| `SshManager::open_connection` async connect flow | TODO comment in place | SSH integration pass |
| `CredentialManager` → `LinuxCredentialStore` wiring | `CredentialManager` still has `// TODO: store:` | SSH integration pass |
| SecretService integration test (store/get/delete round-trip) | Requires D-Bus + keyring daemon | Credentials milestone |

---

## 5. Security Findings

| ID | Description | Severity | Status |
|---|---|---|---|
| FINDING-CLIP-001 | `copy_to_clipboard` had no size limit — direct arboard call could accept multi-MB payloads from the WebView. Fixed with `MAX_CLIPBOARD_LEN = 16 MiB`. | Medium | Fixed |
| FINDING-KH-001 | `known_hosts.rs` was a stub — TOFU host key verification was entirely unimplemented. Now implemented with `Trusted`/`Mismatch`/`Unknown` distinction. | Critical | Partially addressed (unit-tested; real TCP flow still missing) |
| FINDING-CRED-001 | `is_available()` was hardcoded `false` — SecretService was never attempted. Now implemented with real D-Bus probe + graceful fallback. | High | Fixed |

---

## 6. Test Count Summary

| Category | Before sprint | After sprint | Delta |
|---|---|---|---|
| Rust unit tests (nextest, normal) | 167 | 208 | +41 |
| Rust ignored tests (load security) | 0 | 3 | +3 |
| Frontend tests (vitest) | 110 | 110 | 0 |
| E2E scenarios (written) | 2 | 10 | +8 |
| E2E scenarios (executable) | 0 | 0 | 0 (build deferred) |
